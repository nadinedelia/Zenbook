<div class="panel">
  <%= form_for @post do |form| %> <% if @post.errors.any? %>
  <div id="error_explanation">
    <h2>
      <%= pluralize(@post.errors.count, "error") %> prohibited this post from
      being saved:
    </h2>
    <ul>
      <% @post.errors.full_messages.each do |msg| %>
      <li><%= msg %></li>
      <% end %>
    </ul>
  </div>
  <% end %> <%= form.label :message %> <%= form.text_area :message, :class =>
  "form-control", :placeholder => 'What are you thinking?' %><br />

  <%= form.submit "Share", :class => "btn btn-sm btn-primary pull-right" %> <%
  end %>
</div>
